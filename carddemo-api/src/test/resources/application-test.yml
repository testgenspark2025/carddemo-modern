spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/carddemo_test
    username: carddemo
    password: carddemo
  flyway:
    enabled: true
    clean-disabled: false
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

carddemo:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret-test-secret-test
    expiration-minutes: 5
  data:
    dir: file:/home/factory-user/repos/carddemo/app/data/ASCII
